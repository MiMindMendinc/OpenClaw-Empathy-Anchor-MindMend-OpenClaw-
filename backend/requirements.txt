Flask==3.0.0
Flask-CORS==4.0.0
PyJWT==2.8.0
python-dotenv==1.0.0

# Optional NLP (can be enabled later)
# spacy==3.7.2

# Testing
pytest==7.4.3
pytest-flask==1.3.0

# For production deployment
gunicorn==22.0.0  # Updated to fix CVE vulnerabilities (HTTP smuggling)
